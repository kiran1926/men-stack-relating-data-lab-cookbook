<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Link to the custom styles.css file -->
    <link rel="stylesheet" href="/css/style.css">
    <title>
        <%= recipe.name %>
    </title>
</head>

<body>
    <%- include("../partials/_navbar.ejs") %>

    <div class="container recipe-container">
        <h1 class="text-center recipe-title">
            <%= recipe.name %>
        </h1>

        <div class="recipe-section">
            <h2>Ingredients</h2>
            <ul class="ingredient-list">
                <% recipe.ingredients.forEach(ingredient => { %>
                    <li><%= ingredient.name %></li>
                <% }) %>
            </ul>
        </div>

        <div class="recipe-section">
            <h2>Instructions</h2>
            <p><%= recipe.instructions %></p>
        </div>

        <div class="action-buttons">
            <a href="/users/<%= user._id %>/recipes/<%= recipe._id %>/edit" class="btn btn-primary btn-action">Edit</a>

            <form action="/users/<%= user._id %>/recipes/<%= recipe._id %>?_method=DELETE" method="POST" class="mt-2">
                <button type="submit" class="btn btn-danger btn-action">Delete</button>
            </form>
        </div>
    </div>

</body>

</html>